# Fastlane Match Configuration
# See: https://docs.fastlane.tools/actions/match/

git_url("https://github.com/comtammatu/certificates.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["com.comtammatu.app"])

# username("your@email.com") # Your Apple Developer Portal username
